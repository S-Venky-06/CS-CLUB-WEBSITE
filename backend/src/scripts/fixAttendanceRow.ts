// Retired: this one-off repair targets a legacy column layout and corrupts current records.
throw new Error("This legacy repair is disabled. Back up the worksheet and use the admin registration interface to update attendance; migrate legacy rows explicitly.");
export {};
